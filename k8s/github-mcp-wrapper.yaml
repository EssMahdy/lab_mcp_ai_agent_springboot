apiVersion: apps/v1
kind: Deployment
metadata:
  name: github-mcp-wrapper
  namespace: lab-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: github-mcp-wrapper
  template:
    metadata:
      labels:
        app: github-mcp-wrapper
    spec:
      containers:
        - name: github-mcp-wrapper
          image: mcp-github-http-wrapper:dev
          imagePullPolicy: IfNotPresent
          env:
            - name: GITHUB_PERSONAL_ACCESS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: agent-secrets
                  key: GITHUB_TOKEN
          ports:
            - containerPort: 3333
---
apiVersion: v1
kind: Service
metadata:
  name: github-mcp-wrapper
  namespace: lab-agent
spec:
  selector:
    app: github-mcp-wrapper
  ports:
    - name: http
      port: 3333
      targetPort: 3333
